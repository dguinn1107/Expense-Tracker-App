using Microsoft.Maui.Controls;

namespace ExpenseTracker.Views
{
    public partial class HomePage : ContentPage
    {
        public HomePage()
        {
            InitializeComponent();
        }

        // You can handle button clicks and other events here, for example:
        private void OnAddExpenseClicked(object sender, EventArgs e)
        {
            // Your logic for adding an expense
        }

        private void OnHistoryClicked(object sender, EventArgs e)
        {
            // Your logic for showing history
        }

        private void OnReportsClicked(object sender, EventArgs e)
        {
            // Your logic for reports
        }
    }
}
